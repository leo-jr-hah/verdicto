# Verdicto — Project Context

## Architecture
- **dashboard/** — React 19 + TypeScript + Vite frontend
- **agents/** — Backend: orchestrator + 5 AI agents (2 valuation + 3 jurors)
- **contracts/** — Rust smart contracts (Odra framework) — ReputationRegistry, VotingContract
- Deployed: Vercel (frontend) + Railway (backend)

## Live URLs
- Frontend: https://verdicto.xyz
- Backend: https://verdicto-production.up.railway.app
- GitHub: https://github.com/leo-jr-hah/casper-rwa-court

## Wallet Integration (CRITICAL)
- **Official Casper Wallet SDK** (injected by extension) — NO external npm packages needed
- `window.CasperWalletProvider` is the factory function (NOT `window.casper` which was deprecated)
- `window.CasperWalletEventTypes` has event names: Connected, Disconnected, ActiveKeyChanged
- Flow: `const provider = CasperWalletProvider()` → `provider.requestConnection()` → listen for events → `provider.getActivePublicKey()`
- `provider.isConnected()`, `provider.sign()`, `provider.signMessage()`, `provider.disconnect()`
- **CRITICAL: provider.on() does NOT exist!** Events are DOM CustomEvents on `window`:
  - `window.addEventListener(CasperWalletEventTypes.Connected, handler)`
  - `event.detail` is JSON: `{ activeKey, isConnected, isLocked }`
- **Payment flow:** `signPayment()` in CSPRClickContext creates a native transfer deploy via `casper-js-sdk`, asks wallet to sign it, returns base64 payment proof
- Chrome Web Store: `https://chromewebstore.google.com/detail/casper-wallet/abkahkcbhngaebpcgfmhkoioedceoigp?hl=en`
- Testnet Faucet: `https://testnet.cspr.live/tools/faucet`

## Products (6 total)
1. **Assess** — AI asset valuation (dual-agent, 3 jurors, 2.5 CSPR fee) ✅
2. **Borrow** — Lend against assessed assets (AI LTV, real CSPR disbursement, 5 CSPR fee) ✅
3. **Insure** — Insure assets against value loss (AI risk scoring, claims with revaluation, 3 CSPR fee) ✅
4. **Predict** — Prediction market resolution (1 CSPR fee) ✅
5. **Oracle** — Verdict dashboard (read-only, stats, verdict history) ✅
6. **Disputes** — Challenge verdicts (5 CSPR stake, adversarial re-trial) ✅

## Key Files
- `dashboard/src/contexts/CSPRClickContext.tsx` — wallet provider
- `dashboard/src/components/PaymentModal.tsx` — shared payment modal (all products)
- `dashboard/src/components/WalletConnectButton.tsx` — wallet UI
- `dashboard/src/layouts/Layout.tsx` — sidebar navigation
- `dashboard/src/services/api.ts` — all backend API calls + types
- `dashboard/src/config/casper.ts` — fees, wallet address
- `agents/orchestrator/index.ts` — all backend endpoints
- `agents/shared/trust-framework.ts` — trust scoring
- `agents/shared/verifiable-execution.ts` — HMAC receipt chains
- `agents/shared/x402-middleware.ts` — x402 payment verification

## Backend Endpoints (35 total)
- `POST /api/assess` — Run asset assessment
- `POST /api/assessments/start` — Start assessment session
- `GET /api/assess/demo` — Demo assets
- `POST /api/predict` — Prediction market
- `POST /api/loans/create` — Create loan (x402 gated)
- `GET /api/loans` — List loans
- `GET /api/loans/:id` — Get loan
- `POST /api/loans/:id/repay` — Repay loan
- `POST /api/loans/:id/revalue` — Revalue collateral
- `POST /api/insurance/create` — Create policy (x402 gated)
- `GET /api/insurance` — List policies
- `GET /api/insurance/:id` — Get policy
- `POST /api/insurance/:id/claim` — File claim
- `GET /api/oracle/verdicts` — List verdicts
- `GET /api/oracle/verdict/:assetId` — Get verdict by asset
- `GET /api/oracle/stats` — Oracle statistics
- `POST /api/oracle/dispute` — File dispute (x402 gated, 5 CSPR stake)
- `POST /api/oracle/disputes/:id/retrial` — Trigger re-trial
- `GET /api/oracle/disputes` — List disputes
- `GET /api/oracle/disputes/:id` — Get dispute
- `GET /api/reputation` — Agent reputation scores
- `GET /api/transactions` — Transaction history
- `GET /api/contract-state` — Live contract state
- `POST /api/receipts/verify` — Verify HMAC receipt
- `POST /api/relay-deploy` — Relay signed deploy
- `GET /health` — Health check

## Fees
| Product | Fee (CSPR) |
|---------|-----------|
| Assess | 2.5 |
| Predict | 1 |
| Borrow | 5 |
| Insure | 3 |
| Dispute | 5 (stake) |

## x402 Payment Proof Format (CRITICAL)
- Wallet creates nested proofs: `{ scheme, payload: { deploy, payer, amount, network }, deployHash, broadcast }`
- Backend MUST use `decoded.amount || decoded.payload?.amount` (NOT just `decoded.amount`)
- Same for payer: `decoded.payer || decoded.payload?.payer`
- Same for txHash: `decoded.txHash || decoded.deployHash`
- **On-chain verification uses retry with backoff** (3 attempts, 3s/6s delays) because testnet deploys can take 10-30s to propagate
- **All x402 errors include actionable `hint` field** — frontend displays via `signErrorHint` / `errorHint` with Lightbulb icon
- **All emojis removed from UI** — replaced with Lucide icons (AlertTriangle, BarChart3, Scale, Link2, Lightbulb, Building2, Palette, Gem, TrendingUp)
- **BorrowView verdict card** — `loanRejected` state shows inline rejection verdict instead of generic error banner

## Tests
- 55/61 tests passing (vitest)
- 4 test suites: HMAC chain, trust scoring, agent orchestration, x402 integration
- 6 x402 integration tests failing (nested proof format edge cases)

## Environment
- OS: macOS (Apple Silicon)
- Node: 20+
- Deploy: Railway (backend), Vercel (frontend)

## Recent Changes (June 25, 2026)
- Removed 22 dev artifact files from git tracking
- Updated .gitignore to prevent re-tracking
- Rewrote all root docs: README.md, ARCHITECTURE.md, CLAUDE.md, PROJECT_REPORT.md, VALIDATOR_PROMPT.md
- Updated dashboard/README.md with all 12 routes
- All docs now reflect current "Verdicto" branding and 6-product architecture

## E2E Test Report ✅ (June 26, 2026)
- **72/72 test cases pass** against live production (Vercel + Railway)
- All 4 products (Assess, Borrow, Insure, Predict) work with real AI agents
- Oracle + Disputes with adversarial re-trials fully functional
- CORS verified for verdicto.xyz, www.verdicto.xyz, localhost:5173
- All 13 frontend routes return HTTP 200
- All input validation (10 edge cases) working
- TypeScript clean, 61/61 unit tests pass
- Report: `E2E_TEST_REPORT.md`

## Supabase DB Integration ✅ (June 26, 2026)
- **`agents/shared/db.ts`** — Full Supabase CRUD layer (assessments, loans, insurance, oracle_verdicts, disputes)
  - Graceful fallback when SUPABASE_URL not set (local dev)
  - Generic upsert/insert/select helpers with error handling
- **Write paths** — All writes are fire-and-forget `.catch()` (non-blocking):
  - `casper-contracts.ts`: `db.saveVerdict()` (×3), `db.saveDispute()` (×4)
  - `orchestrator/index.ts`: `db.saveAssessment()`, `db.saveLoan()`, `db.saveInsurance()`
- **Read paths** — In-memory first, Supabase fallback after restart:
  - `getAllVerdicts()`, `getOracleStats()`, `getDispute()`, `getAllDisputes()`, `getDisputesForAsset()` → async with DB fallback
  - `/api/loans`, `/api/loans/:id` → falls back to `db.getAllLoans()`, `db.getLoan()`
  - `/api/insurance`, `/api/insurance/:id` → falls back to `db.getAllInsurance()`, `db.getInsurance()`
- **SQL schema**: `agents/shared/supabase-schema.sql` — all 5 tables with indexes
- **Env vars**: `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (CRITICAL: URL must NOT include `/rest/v1/`)
- **TypeScript**: `npx tsc --noEmit` clean, `npm test` 61/61 pass

## Recent Fixes (2026-06-27)
- **Railway crash fix**: `@supabase/supabase-js` v2.108.2 throws on Node <22 without native WebSocket. Fix: dynamic `import('ws')` + `{ realtime: { transport: ws } }` in `createClient()`. Made `getClient()` async.
- **TS build errors fixed**: Removed unused imports/functions in BorrowView, InsureView. Added `escrowReleaseTxHash` to `LoanCreateResponse`.
- **x402 test assertions updated**: 7 assertions updated to match improved error messages.
- **All 61 tests pass**, `npm run build` clean, Railway deployed and healthy.

## Pending
- LLM agents fall back to deterministic estimates on Railway (likely missing GROQ_API_KEY or MIMO_API_KEY env vars)
